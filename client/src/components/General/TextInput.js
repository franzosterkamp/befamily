import styled from '@emotion/styled';

const TextInput = styled.textarea`
  font-weight: bold;
  font-size: 0.7rem;
  resize: none;
  border-radius: 10px;
  outline: none;
`;

export default TextInput;
